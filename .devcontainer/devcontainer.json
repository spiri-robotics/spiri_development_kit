{
    "name": "Spiri SDK dev environment",
    "privileged": true,
    "remoteUser": "vscode",
    "build": {
        "dockerfile": "../Dockerfile",
        "target": "devcontainer",
        "args": {
            "USERNAME": "vscode"
        }
    },
    "features": {
        "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {}
    },    
    "workspaceFolder": "/home/ws",
    "workspaceMount": "source=${localWorkspaceFolder},target=/home/ws,type=bind",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.cpptools-themes",
                "twxs.cmake",
                "donjayamanne.python-extension-pack",
                "eamodio.gitlens",
                "ms-iot.vscode-ros"
            ]
        }
    },
    "containerEnv": {
        "DISPLAY": "${env:DISPLAY}",
        "SDK_ROOT": "${localWorkspaceFolder}",
        "QT_X11_NO_MITSHM": "1",
        "XAUTHORITY": "/tmp/.docker.xauth"
    },
    "runArgs": [
        "--net=host",
        "--pid=host",
        "--ipc=host",
        "--security-opt=seccomp=unconfined"
    ],
    "mounts": [
        "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind,consistency=cached",
        "source=/dev/dri,target=/dev/dri,type=bind,consistency=cached",
        "source=${env:XAUTHORITY},target=/tmp/.docker.xauth,type=bind,consistency=cached",
        "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind",
        "source=/tmp/,target=/tmp/,type=bind,consistency=cached",
        "source=${localWorkspaceFolder},target=${localWorkspaceFolder},type=bind,consistency=cached",
    ],
    "postCreateCommand": "uv sync --locked"
}
